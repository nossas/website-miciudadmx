machine:
  timezone:
    America/Sao_Paulo # Set the timezone
  php:
    version: 7.0.4

deployment:
  production:
    branch: master
    commands:
      - git remote add dokku dokku@reboo.org:miciudadmx-org
      - git push dokku master
